// Initialize a variable
const num = 3

if (num % 2 === 0) {
  console.log(`The number ${num} is even`) // @logs message indicatimg number is even
} else {
  console.log(`The number ${num} is odd`) // @logs message indicatimg number is odd
}
