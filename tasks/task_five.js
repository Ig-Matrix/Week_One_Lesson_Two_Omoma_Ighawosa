const num = 11

const message = (num > 10) ? `${num} is greater than 10` : `${num} is less than or equal to 10`

console.log(message) // @logs message indicating message depending on the condition, in this case : 11 is greater than 10
