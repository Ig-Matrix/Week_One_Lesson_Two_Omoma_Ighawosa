const num = -2 

if (num < 0) {
  console.log(`${num} is negative`) // @logs message indicating number is negative
} else if (num > 0) {
  console.log(`${num} is positive`) // @logs message indicating number is positive
} else {
  console.log(`${num} is zero`) // @logs message indicating number is zero
}
